
Build
docker build . -t mywebapp

Verify  image
$ docker image ls

Run image:
docker run -p 9000:8080 mywebapp

Detach Mode:
docker run -d -p 9000:8080 mywebapp

docker ps
docker stop <container id>
docker rmi $(docker images -a -q) -f